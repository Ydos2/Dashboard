import Cookies from 'universal-cookie';

export var widgetConf = [
    {id: 0, nameWidget: 'weather', stateWidget: "false"},
    {id: 1, nameWidget: 'crypto', stateWidget: "false"}
  ];

export const cookies = new Cookies();
